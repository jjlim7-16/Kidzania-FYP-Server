const os = require('os')

module.exports = {
	// Localhost
	// hostname: 'localhost'

	// Hamachi
	hostname: '25.37.100.106'

	// Public IP Address
	// hostname: os.networkInterfaces()['Wi-Fi'][1].address
}